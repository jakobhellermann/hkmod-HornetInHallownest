// The decompiled files' types were rewritten from `namespace GlobalEnums`/`GlobalSettings` into `namespace Silksong`
// (uniform-namespace policy in extract.sh), but their `using GlobalEnums;` / `using GlobalSettings;` directives
// remain. These empty declarations keep those namespaces existing so the (now dead) usings still resolve.
namespace GlobalEnums { }
namespace GlobalSettings { }
