// HeroController has `using GlobalSettings;` (a Silksong namespace HK lacks). This makes the namespace exist so the
// directive resolves. The class is a stub — HeroController only imports the namespace, it doesn't use members here.
namespace GlobalSettings;

public class GlobalSettings { }
